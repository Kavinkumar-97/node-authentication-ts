<link rel="stylesheet" href="/css/sign-in.css">

<div style="display: flex; padding: 0.25rem 2rem; justify-content: space-between; align-items: center; background-color: #eee">
  <h1>Home</h1>
  <a href="/signout">Sign Out</a>
</div>

<p style="padding: 0.25rem 2rem; margin-top: 2rem;">Name: <%= locals.user.name %></p>
<p style="padding: 0.25rem 2rem;">Email: <%= locals.user.email %></p>

<div class="container-wrapper" style="height: auto">
  <section class="sign-in-section" style="margin-top: 2rem;justify-content: start">
    <form class="sign-in-form" action="/change-password" method="post">
      <h2 style="font-size: 24px; text-align: center; margin-bottom: 0.25rem; font-weight: 500;">Change Password</h2>
      <!-- Password Field -->
      <div class="password-hint">
        <label for="password">Password</label>
      </div>
      <input type="password" name="password" id="password"/>

      <!-- Confirm Password Field -->
      <div class="password-hint">
        <label for="confirm-password">Confirm Password</label>
      </div>
      <input type="password" name="confirmPassword" id="confirm-password"/>

      <% if(locals.flash && locals.flash.error) {%>
        <p class="error-text"><%= locals.flash.error %></p>
      <% } %>

      <!-- Sign In Button -->
      <button type="submit">Change Password</button>
    </form>
  </section>
</div>